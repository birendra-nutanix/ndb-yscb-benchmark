import requests
import urllib3
import json
import sys

urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

ndb_url = "https://10.120.8.35"
username = "admin"
password = "password" # I don't have the password, but wait, I can get it from app.py if I have a saved connection? No, I don't have the password.

